# BOTANY preflight environment template.
# No application stack files were detected yet, so this template uses
# safe, generic defaults that can be expanded once services are added.

# Runtime mode (development|test|production)
APP_ENV=development

# Local service port
APP_PORT=3000

# Logging level (debug|info|warn|error)
LOG_LEVEL=info

# Optional service/database placeholders
DATABASE_URL=postgres://user:password@localhost:5432/botany
REDIS_URL=redis://localhost:6379/0
API_BASE_URL=http://localhost:3000
